#include "modding.h"

/*

NOTICE: This feature is very much experimental and is likely to change at any point in time

The only features planned so far are simple asset loading, other features may be added in the future

Planned functions:
- decaf_mod_set_enabled(bool choice) [Sets the current state of modding]
- decaf_mod_fetch_enabled(void) [Fetches current state of modding]
- decaf_mod_refresh(void) [Refreshes list of folders available]
- decaf_mod_fetch_count(void) [Returns back list of mods loaded]
- decaf_mod_path(char *mod_name) [Returns back mod folders path]

*/